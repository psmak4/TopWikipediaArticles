/**
 * Represents an article with its properties.
 * @interface
 */
interface Article {
	article: string // The title of the article.
	views: number // The number of views the article has.
	rank: number // The rank of the article.
}

export default Article
